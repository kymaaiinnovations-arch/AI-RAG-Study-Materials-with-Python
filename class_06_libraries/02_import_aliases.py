# Import NumPy and Pandas using their common short aliases.
import numpy as np
import pandas as pd

# Print the installed library versions to confirm the imports work.
print("NumPy version:", np.__version__)
print("Pandas version:", pd.__version__)
