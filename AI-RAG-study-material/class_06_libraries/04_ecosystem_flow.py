# Build a simple list showing how the Python AI/data stack flows.
steps = ["NumPy (Math)", "Pandas (Tables)", "Matplotlib (Plots)", "Scikit-Learn (ML)", "LangChain (RAG/AI)"]

# Join the list items with arrows to visualize the pipeline.
print(" -> ".join(steps))
